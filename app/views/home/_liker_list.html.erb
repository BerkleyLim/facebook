<% if user_signed_in? %>
  <% if Likeit.where(user_id: current_user.id, posting_id: id).take == nil %>
    <%= link_to '좋아요', home_likeit_path(name: id), class: "likeit", remote: true %>
  <% else %>
    <%= link_to '좋아요 취소', home_likeit_path(name: id), class: "likeit", remote: true %>
  <% end %>
<% else %>
  <%= link_to '좋아요' %>
<% end %>

<br>

<% members.each do |m| %>
    <%= m.email %>,
<% end %>